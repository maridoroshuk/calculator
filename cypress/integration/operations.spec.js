describe('operations', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  it('add operation', () => {
    // go to the home page
    // click digit button(s)
    // click operation button
    // click eval or another
    // show answer
  })
})
